# Include only packages necessary in development environment
name: base
channels:
  - defaults
dependencies:
  - ipykernel
  - ipywidgets
  - pip
  - pip:
    - pep8-naming
    - flake8-isort
prefix: /opt/conda