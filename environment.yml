# Include only packages necessary in development environment
name: base
channels:
  - defaults
  # - conda-forge
dependencies:
  - pydantic
  - python-dotenv
  - pip:
    - transformers
    - datasets
    - accelerate
    - librosa
    - soundfile
prefix: /opt/conda