# Include only packages necessary in development environment
name: base
channels:
  - defaults
dependencies:
  - pydantic
  - python-dotenv
  - transformers
  - pip
    - accelerate
prefix: /opt/conda